## Packages
date-fns | Date formatting and calculation for expiration warnings
framer-motion | Smooth animations and page transitions
lucide-react | Beautiful icons
react-hook-form | Form state management
@hookform/resolvers | Zod validation for forms
zod | Schema validation

## Notes
- Tailwind Config requires extending font family to 'Outfit' (display) and 'Plus Jakarta Sans' (sans).
- Integration assumes /api/auth/login expects { customerCode }.
- File upload for receipts hits /api/receipts/upload with multipart/form-data.
